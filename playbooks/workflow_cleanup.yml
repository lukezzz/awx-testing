---
- name: Workflow cleanup
  hosts: web
  gather_facts: false

  tasks:
    - name: Cleanup message
      ansible.builtin.debug:
        msg: "Workflow cleanup executed on {{ inventory_hostname }}"
